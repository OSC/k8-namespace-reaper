{{- if .Values.namespace.create -}}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Release.Namespace | quote }}
  labels:
    {{- include "k8-namespace-reaper.labels" . | nindent 4 }}
{{- end }}
